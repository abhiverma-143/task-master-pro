# 🚀 Task Master Pro

A modern, full-stack Task Management application built to organize daily activities efficiently. Features a drag-and-drop interface, real-time status updates, and a celebratory confetti effect upon completion!

## ✨ Features
- **Kanban Style Board:** Organize tasks into Todo, In Progress, and Done.
- **Drag & Drop:** Smooth drag-and-drop functionality using `@hello-pangea/dnd`.
- **Celebration Effect:** 🎉 Confetti animation when a task is moved to "Done".
- **Glassmorphism Design:** Modern UI with blurred glass effects and dynamic column colors.
- **Full CRUD:** Create, Read, Update (Status), and Delete tasks.
- **Responsive:** Works seamlessly on different screen sizes.

## 🛠️ Tech Stack

### Frontend 🎨
- **React.js:** Component-based UI.
- **Axios:** API Integration.
- **Hello Pangea DnD:** For Drag and Drop logic.
- **React Confetti:** For visual rewards.
- **CSS3:** Custom styling with Glassmorphism.

### Backend ⚙️
- **Java Spring Boot:** REST API development.
- **PostgreSQL:** Relational database for data persistence.
- **Spring Data JPA:** Hibernate ORM for database interactions.
- **Cors Configuration:** Secure frontend-backend communication.

## 🚀 How to Run Locally

### 1. Backend Setup (Java)
1. Open `task-manager` in IntelliJ IDEA or VS Code.
2. Configure `application.properties` with your PostgreSQL credentials.
3. Run the Spring Boot application.
4. Server will start at: `http://localhost:8081`.

### 2. Frontend Setup (React)
1. Open a terminal in the `task-ui` folder.
2. Install dependencies:
   ```bash
   npm install

   App will open at: http://localhost:3000.


📌 Future Improvements
[ ] User Authentication (Login/Signup).
[ ] Dark Mode Toggle.
[ ] Task Edit Functionality.
[ ] Priority Tags (High/Medium/Low).

Developed with ❤️ by Abhishek

---

### **Step 3: Update GitHub (Push Changes)** 🚀
Ab kyunki humne nayi file add ki hai, humein ise GitHub par bhejna padega.

Terminal mein ye 3 commands chalao:

1.  **Add:**
    ```bash
    git add .
    ```
2.  **Commit:**
    ```bash
    git commit -m "Added Professional README"
    ```
3.  **Push:**
    ```bash
    git push
    ```
---